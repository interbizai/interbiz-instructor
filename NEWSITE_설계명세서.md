# 인터PICK 재창조 명세서 (새 사이트 구축용)

> **이 문서의 용도**: 완전히 새로운 사이트를 처음부터 만드는 클로드(디자인/개발 세션)에게 그대로 전달하는 설계도.
> **전제**: 화면·코드는 100% 새로 창조한다. 단, **데이터(DB)와 저장된 영상·평가 이력은 기존 것을 그대로 이어받는다.**
> 기존 사이트(interpick.vercel.app)는 새 사이트가 완성될 때까지 병행 운영된다.
> 작성일 2026-08-03 · 기존 운영본 전수 분석 기준

---

# 1. 제품 정의

**강사가 강의 영상을 올리면 → AI가 조직의 평가표로 채점하고 → 점수가 쌓여 강사 개인의 성장 이력이 되는 강사 역량 평가 플랫폼.**

- 회사: 인터비즈 (interbiz) — 가전 현장강사 교육 회사
- 사용자: **강사**(영상 제출·결과 확인·성장 추적) / **관리자=사장님**(평가 기준·계정·비용 관리)
- 조직 2개가 한 시스템을 쓰되 데이터 완전 분리: **`LG전자 강사`** / **`하이케어솔루션`** (org_name 컬럼 기준)
- 언어: 한국어 전용. 전문용어 없이 사장님이 바로 이해할 수 있는 문구.

## 확정된 결정 (2026-08-03)

| 항목 | 결정 |
|---|---|
| 기능 범위 | 영상·스트리밍·스피치·시나리오 **전부 유지**, 시스템 구조는 새로 설계 |
| 뱃지(91종) | **완전 삭제** — 새 사이트에 만들지 않음 |
| 랭킹·강사 비교 | **보류** — 일단 만들지 않음 (나중에 결정) |
| AI 모델 | **이원화** — 교육맞춤평가(정식)=Gemini Pro · AI독자(참고)=Gemini Flash |
| 기기 | **PC 주력** + 모바일 반응형 필수 |
| 디자인 | 컬러·톤 전부 새로 창조 (이 문서는 기능만 정의, 디자인 자유) |

---

# 2. 화면 구성 (5개 메뉴)

```
① 홈        내 최근 점수 요약 · 진행 중인 분석 상태 · 공지 · 일정
② AI 평가   [영상 파일 | 웹캠 녹화 | 음성(스피치) | 시나리오 첨삭] 한 화면에서 선택
③ 내 역량   점수 추이 그래프 · 대항목 레이더 · 평가 이력 목록 · 개선 추적
④ 자료실    교안(PPT/PDF) 관리 · 체크리스트(평가표 엑셀) 관리
⑤ 관리자    [사용자] [평가 기준] [현황·비용] 3탭
```

### 설계 원칙
1. **요약 → 상세**: 모든 화면은 3초 안에 핵심 숫자가 보이고, 상세는 펼쳐서 본다.
2. **한 화면 한 목적**: 한 페이지에 정보 10종을 쌓지 않는다 (기존 사이트의 최대 문제).
3. 5단계 채점 색상·용어를 전 화면 통일: **4~5점 초록 · 3점 주황 · 1~2점 빨강** (비율로는 70%↑ / 50%↑ / 미만).
4. 장식 기능(파티클, 자동 사진 스크롤 등) 배제 — 숫자와 피드백이 주인공.

### 결과 화면 (가장 중요) 구성 순서
```
[3줄 요약]   총점 · 최대 강점 1개 · 이번 주 실행할 액션 1개
[점수 카드]  교육맞춤평가 79점 | AI 독자 73점 (+ 지난 평가 대비 ▲6)
[레이더/달성도] ── 펼치기
[항목별 상세]   ── 펼치기 (15개 항목: 점수·판정·영상시점 MM:SS·분석·솔루션, 시점 클릭 시 해당 장면 재생)
[음성 분석]     ── 펼치기 (성량 dB·말속도 WPM·음높이·반복어 카운트)
[개선 추적]     지난 지적 → 이번에 개선됐는지 자동 비교  ← 신규 기능
```

### 새로 추가할 기능 (기존에 없음)
| 기능 | 내용 |
|---|---|
| **백그라운드 분석** | 업로드 → 즉시 "접수 완료" → 강사는 창 닫아도 됨 → 완료 시 알림. (기존은 5~13분 창 열고 대기, 504 타임아웃 빈발) |
| **개선 추적** | 같은 강사의 직전 평가와 항목별 비교 — "지난 지적 3건 중 2건 개선" |
| **관리자 한눈 대시보드** | 조직 평균 점수·하위권 강사·미제출자 |
| 관리자 수기 수정 | 점수·분석·솔루션 인라인 수정, 총점·그래프 즉시 재계산 (기존 기능 유지) |

### 만들지 않는 것 (기존에 있었지만 제외)
뱃지 91종 · 랭킹/비교 · 포트폴리오/경력 관리 · 한줄 시그니처 · 앱설치 QR 포스터 · 우측 추천영상 패널 · 전체검색 · 히어로 사진 자동 스크롤

---

# 3. 데이터 — 기존 Supabase를 그대로 이어받는다

**새 DB를 만들지 않는다.** 기존 Supabase 프로젝트에 그대로 연결하면 수년치 평가 이력·사용자·영상이 즉시 살아있다. (접속 정보는 기존 Vercel 환경변수에서 복사)

## 핵심 테이블 (실제 운영 중인 구조)

| 테이블 | 역할 | 핵심 컬럼 |
|---|---|---|
| `users` | 강사·관리자 계정 | id(BIGINT), name, email, password(해시), org_name, team, position, photo, is_sub_admin |
| `users_safe` | photo 제외한 조회용 뷰 | 목록 조회는 이걸 사용 (photo가 1~3MB라 분리) |
| `videos` | 업로드된 강의 영상 | id, user_id, title, file_path(GCS URL), **video_gcs_uri**(gs:// — AI 분석용), mime, edu_type, product, thumbnail_url, org_name |
| `evaluations` | **AI 평가 결과 (가장 중요)** | video_id, user_id, **eval_type**('평가안기준'\|'AI독자'), overall_score, **sub_scores JSONB**, categories JSONB, good/bad/upgrade JSONB, scenarios, level_tips, habits(반복어), engagement_gaps, mood, decibel, tempo, speech_report JSONB, checklist_id, edu_file_url, org_name |
| `voice_evals` | 스피치(음성) 평가 | user_id, title, score, tone, student_count, result_data JSONB, org_name |
| `checklist_files` | 평가표(체크리스트) 파일 | id, name, type(교육종류명\|'standard'\|'speech'), file_url, category, org_name |
| `checklist_items` | 평가표 세부 항목 | checklist_id, category(대항목), sub_item, criterion, max_score(배점, 합계100), detail, **scale_max**, **levels JSONB**(5단계 기준문장 {"5":"..","1":".."}), sort_order |
| `timestamps` | 영상 시점 피드백 | video_id, t, type(good/bad/tip), text |
| `calendar_events` | 일정 | org_name 분리 |
| `pick_notices` / `pick_contents` / `learning_links` / `recommended_videos` / `pick_featured_videos` | 홈 콘텐츠·공지 | org_name 분리 |
| `notifications` | 알림 | user_id, type, title, body, link, read_at, org_name |
| `app_settings` | 키-값 설정 저장소 | key(PK), value — 히어로 문구, 시나리오 초안, **ai_coaching_blocked_<조직>**(AI 사용 중지 스위치) 등 |
| `vertex_cache` | AI 분석 결과 캐시 | cache_key(영상+체크리스트+프롬프트버전 해시), result JSONB — 같은 분석 재요청 시 0원 |
| `portfolio` | (구기능 데이터 — 새 화면은 안 만들지만 데이터 보존) | |

## sub_scores JSONB 구조 (평가 1건의 항목별 데이터 — 호환 필수)
```json
{
  "category": "1.내용 전문성",     // 대항목
  "sub_item": "제품 숙지도",       // 세부항목
  "criterion": "평가 기준 문장",
  "max_score": 10,                // 배점 (전체 합 100)
  "score": 8,                     // 획득 점수
  "level": "good|normal|bad|na",
  "level_score": 4,               // 5단계 원점수 (5~1)
  "level_name": "우수",
  "manual": true,                 // 관리자 수기 수정 표시 — true면 재계산 금지!
  "timestamp": "12:30-14:00",     // 영상 시점
  "analysis": "...", "solution": "...", "evidence": "..."
}
```

## 데이터 규칙 (반드시 지킬 것)
1. **모든 조회·저장에 org_name 필터** — 조직 간 데이터가 절대 섞이면 안 됨.
2. `manual: true`인 항목의 점수는 **어떤 재계산에서도 건드리지 않는다** (수기 수정 보존).
3. 기존 평가 이력(level_score 없는 옛 데이터)은 점수 재계산 없이 그대로 표시.
4. RLS가 꺼져 있고 anon 키로 직접 DB 접근하는 게 기존 구조의 약점 — **새 사이트는 서버 경유(API) 접근으로 개선 권장.**

---

# 4. AI 채점 엔진 — 이식할 핵심 자산

기존 `api/vertex-analyze.js`(약 1,000줄)에 검증된 채점 로직이 완성돼 있다. **새로 짜지 말고 이식**할 것. 핵심 규칙:

## 4-A. 평가 2종 (영상 1건당)
| 평가 | 모델 | 기준 | 저장 eval_type |
|---|---|---|---|
| **교육맞춤평가** (정식 점수) | `gemini-2.5-pro` | 조직 체크리스트 + 교안 대조 | `'평가안기준'` |
| **AI 독자** (참고 점수) | `gemini-2.5-flash` | AI가 평가안을 스스로 설계해 독립 채점 | `'AI독자'` |

## 4-B. 5단계 앵커 채점 (점수 인플레이션 방지 — 최근 완성한 핵심)
- 모든 항목 **출발점 3점(보통)**. 엑셀의 5단계 기준 문장(levels)과 대조해 1~5점 확정.
- **4점 이상은 영상 시점(MM:SS) + 실제 발화 인용 근거 의무.** 근거 없으면 서버가 자동 강등.
- 분포 상한: 15항목 기준 5점 최대 3개, 4점+5점 합계 최대 7개 (항목 수 비례).
- 강사가 안 한 항목은 na가 아니라 **1점**. na는 '평가 자체가 불가능'할 때만.
- 점수 환산: 5점=배점의 100% · 4점=80% · 3점=60% · 2점=40% · 1점=20%.
- 색상: 70%↑ 초록 · 50%↑ 주황 · 미만 빨강 (전 화면 공통).
- 5단계 기준표 없는 체크리스트도 동일 강도로 채점 (범용 앵커 대체).
- 응답 언어: 순수 한글만 (한자 금지 규칙 포함).

## 4-C. 평가표 엑셀 양식 (관리자 업로드)
열: **대항목 | 세부항목 | 평가기준 | 배점(합계 100 필수) | 평가기준 상세 | 5점(매우 우수) ~ 1점(매우 미흡)**
5점~1점 열은 선택 — 있으면 그 문장이 채점 기준으로 그대로 주입됨.

## 4-D. 영상 처리 파이프라인
1. 브라우저 → GCS **Signed URL 직접 업로드** (서버 경유 X, 크기 무제한) — `api/gcs-upload-url.js` 이식
2. `gs://` 경로를 Gemini에 전달 (fps 0.2 안팎, 영상 길이에 따라 자동 조절)
3. 교안(PPT/Word/Excel)은 **브라우저에서 텍스트만 추출**해 전송 (수백MB → 수KB), 8MB 초과 원본은 GCS로
4. 결과는 `vertex_cache`에 캐시 (같은 분석 재요청 시 무료)

## 4-E. 새 사이트에서 개선할 구조 (권장)
- **백그라운드 작업 큐**: 분석 요청을 DB에 쌓고 별도 처리 → 완료 알림. 서버리스 300초 한도로 인한 504 타임아웃 원천 제거.
- **호출 2번 → 1번 통합 검토**: 같은 영상을 두 평가에 두 번 넣는 대신 한 번에 두 결과 생성 시 비용 추가 절반 절감 가능. (단, Pro/Flash 이원화와는 상충 — 통합 시 모델 하나로 통일 필요. 설계 단계에서 선택)
- **AI 사용 중지 스위치 유지**: `app_settings.ai_coaching_blocked_<조직>` = '1'이면 강사 실행 차단 (서버에서 판정, 관리자는 예외). 이미 운영 중인 규칙 — 새 사이트도 동일 키 사용.

---

# 5. 외부 연동 (기존 자원 그대로)

| 자원 | 용도 | 새 사이트에서 |
|---|---|---|
| Supabase (유료) | DB · 소형 파일 저장 | 같은 프로젝트에 연결 (URL·키는 기존 Vercel 환경변수 복사) |
| Google Cloud Storage | 영상·대형 교안 (버킷 `interbiz-videos`) | 그대로 사용 |
| Vertex AI (기업용) | Gemini Pro/Flash 채점 | 그대로 사용 (서비스 계정 키 재사용) |
| JWT 자체 로그인 | 이메일+비밀번호 → 토큰(sub=사용자id, 관리자는 sub=0 + org 클레임) | 방식 유지 또는 Supabase Auth로 전환 (선택) |

필요 환경변수 (이름 목록 — 값은 기존 Vercel 프로젝트 Settings에서 복사):
`SUPABASE_URL` · `SUPABASE_SERVICE_ROLE_KEY` · `JWT_SECRET` · `GCS_BUCKET` · `GCP_CREDENTIALS_JSON`(또는 `GCP_PROJECT_ID`/`GCP_CLIENT_EMAIL`/`GCP_PRIVATE_KEY`)

## 기존 저장소에서 "복사해갈 가치가 있는" 파일
| 파일 | 내용 |
|---|---|
| `api/vertex-analyze.js` | **채점 엔진 전체** (5단계 앵커 프롬프트·서버 채점 확정·캐시·중지 스위치) |
| `api/gcs-upload-url.js` | GCS 직접 업로드 Signed URL 발급 |
| `api/db/load.js` | 조직 분리 데이터 로드 패턴 (org 강제 필터) 참고 |
| `assets/js/04-admin-edu.js`의 `parseChecklistRows()` | 평가표 엑셀 파서 (5단계 열 인식) |
| `database/migrations/*.sql` | DB 스키마 이력 전체 |

---

# 6. 로그인·권한

| 역할 | 권한 |
|---|---|
| 강사 | 본인 영상 업로드·분석 실행(스위치 ON일 때)·본인 결과/이력 열람·영상 삭제 "요청" |
| 부관리자 (is_sub_admin) | 관리자와 동일 (점수 수기 수정 포함) |
| 관리자 (sub=0) | 전부 + 계정 관리·기준 관리·비용 스위치·영상 삭제 승인. 로그인 시 조직을 선택해 잠금 |

- 조직별 로그인 페이지가 분리돼 있었음 (LG / 하이케어) — 새 사이트에서 방식 자유, 단 **토큰에 조직이 박혀야** 함.
- PWA(홈 화면 설치) 지원이 있었음 — 유지 여부 자유.

---

# 7. 전환 계획

1. 새 사이트를 **새 저장소 + 새 Vercel 프로젝트**로 구축 (기존 사이트는 건드리지 않음)
2. 같은 Supabase/GCS에 연결 → 기존 데이터·이력이 새 화면에 즉시 표시됨
3. 병행 테스트 (관리자 먼저 → 강사 일부 → 전체)
4. 도메인 전환으로 완료. 기존 저장소는 보관용으로 동결

> **주의**: 병행 기간에 두 사이트가 같은 DB에 쓰기를 하므로, 새 사이트도 sub_scores 구조(§3)와 eval_type 문자열('평가안기준'/'AI독자')을 그대로 써야 기존 사이트에서도 결과가 깨지지 않는다.

---

# 8. 비용 요약 (참고)

- 영상 분석 대안 전수조사 결과(2026-07): **영상+음성을 함께 이해하는 API는 사실상 Gemini뿐** (OpenAI 영상 미지원, AWS Nova 음성 미지원, Twelve Labs 2배 비쌈, 중국계 보안 부적합) → Vertex 유지가 결론.
- 현 비용: 40분 영상 1건당 Pro 2회 ≈ 1,500~2,500원 → **이원화(Pro+Flash) 적용 후 약 40% 절감** (기존 사이트에 이미 적용됨).
- 추가 절감 카드: 호출 통합(§4-E) · 백그라운드 큐로 타임아웃 재시도 낭비 제거 · 1년 경과 영상 원본 저가 보관 정책.
